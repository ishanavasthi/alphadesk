"""Backend test suite. Run with `pytest` from inside `backend/`.

A package (rather than a bare directory) so `check_tracing_in_image.py` can be
run as `python -m tests.check_tracing_in_image` inside the Docker image, where
pytest is not installed.
"""
